[project]
name = "transferx"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "cryptography>=47.0.0",
    "segno>=1.6.6",
]

[tool.setuptools]
py-modules = ["main", "server", "logger", "tunnel", "dashboard", "tracker", "web_ui"]